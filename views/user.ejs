<!DOCTYPE html>
<html lang="en">
  
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
</head>
  
<body>
    <h1>USERS</h1>
    <hr>
    <div>
       
            <div>
                <label for="name">Name</label>
                <input type="text" id="name" placeholder=<%= user.name %> 
                       value="" name="name" required>
            </div>
            <div>
                <label for="email">Email</label>
                <input type="text" id="email" placeholder=<%= user.email %>  
                       value="" name="email" required>
            </div>
            <div>
                <label for="name">Phone</label>
                <input type="text" id="phone" placeholder=<%= user.phone %>  
                       value="" name="phone" required>
            </div>
            <div>
                <label for="position_id">Position id</label>
                <input type="text" id="position_id" placeholder=<%= user.position_id %>  
                       value="" name="position_id" required>
            </div>
           
           

            
                <% if(user.photo.data){ %> 
                <img src="data:image/<%= user.photo.contentType %>;base64,
                <%=user.photo.data.toString('base64')%>">
                <% } else{%>
                    <img src=<%= user.photo %>  alt="">
                    <% } %>  
          
            
       
       
       
      
    </div>
  
    <hr>
  
   
</body>
  
</html>